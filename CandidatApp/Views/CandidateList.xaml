<UserControl x:Class="CandidatApp.Views.CandidateList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CandidatApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,0,5,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="gridCandidates" 
                  ItemsSource="{Binding Candidates}"
                  IsReadOnly="True" 
                  AutoGenerateColumns="False"                  
                  RowHeight="40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Visibility="Hidden"/>
                <DataGridTextColumn Header="Ime" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Prezime" Binding="{Binding Surname}" Width="*"/>
                <DataGridTextColumn Header="Uloga" Binding="{Binding Role}" Width="2*"/>
                <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" Width="2*"/>
                <DataGridTextColumn Header="Kontakt telefon" Binding="{Binding Phone}" Width="*"/>
                <DataGridTextColumn Header="Iskustvo (mj)" Binding="{Binding ExpirienceMonths}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
